description: Classic on/off switch purely for testing purposes
tags:
  - all

bucketBy: deviceId

environments:
  staging:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100
  production:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100
