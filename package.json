{
  "name": "@featurevisor/example-cloudflare",
  "private": true,
  "version": "0.0.4",
  "description": "For testing only",
  "scripts": {
    "lint": "featurevisor lint",
    "build": "featurevisor build",
    "test": "featurevisor test",
    "export": "featurevisor catalog export",
    "start": "npm run export && featurevisor catalog serve"
  },
  "devDependencies": {
    "@featurevisor/cli": "^3.2.0",
    "wrangler": "^4.114.0"
  }
}
